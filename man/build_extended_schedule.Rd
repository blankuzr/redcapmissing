% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-schedules.R
\name{build_extended_schedule}
\alias{build_extended_schedule}
\title{Build an extended assessment schedule}
\usage{
build_extended_schedule(rcon, instruments, n_repeat_instances = 1L)
}
\arguments{
\item{rcon}{A \code{redcapAPI} connection inheriting from
\code{redcapApiConnection}, as created by \code{\link[redcapAPI:redcapConnection]{redcapAPI::redcapConnection()}}, or
\code{redcapOfflineConnection}, as created by \code{\link[redcapAPI:offlineConnection]{redcapAPI::offlineConnection()}}
or \code{\link[redcapAPI:readPreservedProject]{redcapAPI::readPreservedProject()}}. It must expose project
information, metadata, instruments, and applicable arms, events,
mappings, and repeat configuration.}

\item{instruments}{A nonempty character vector of unique, nonmissing,
nonblank, unpadded raw REDCap instrument names. Values must exist in the
project. Use \code{\link[=all_instruments]{all_instruments()}} explicitly to request the complete
inventory. \code{NULL} does not mean all instruments.}

\item{n_repeat_instances}{One positive finite whole-number numeric scalar
within R's integer range. Every repeating event or repeating instrument
crossing is expanded to instances \code{1} through \code{n_repeat_instances}.}
}
\value{
A tibble with exactly three columns: character \code{instrument},
character \code{redcap_event_name}, and integer \code{repeat_instance}, in that
order. When no crossing can be produced, the result is a correctly typed
zero-row tibble.
}
\description{
\code{build_extended_schedule()} creates every allowable REDCap event crossing
for the requested instruments. Pass its result to \code{\link[=plan_from_data]{plan_from_data()}} as
\code{extended_schedule} to add those crossings for records observed in the
applicable arm.
}
\details{
In a classic project, every requested instrument has one native eventless
crossing, represented by \code{redcap_event_name = NA_character_}. A
nonrepeating instrument receives \code{repeat_instance = NA_integer_}; a
repeating instrument receives instances \code{1:n_repeat_instances}.

In a longitudinal project, only instrument-event crossings designated by
REDCap are returned. A repeating event or instrument receives instances
\code{1:n_repeat_instances}. The function never creates an eventless
longitudinal crossing.

Requested instrument order determines the primary row order, followed by
REDCap event order and ascending repeat instance. The builder creates
schedule rows, not record-level targets. \code{\link[=plan_from_data]{plan_from_data()}} expands each row
across records observed in its applicable arm and verifies that schedule
instruments are a subset of the plan's \code{instruments}.
}
\section{Conditions}{

Argument, project, and schedule validation failures inherit from
\code{redcapmissing_error}. If one or more valid requested instruments in a
longitudinal project are designated to no event, the function omits them
and emits one \code{redcapmissing_warning_undesignated_extension} warning. The
warning also inherits from \code{redcapmissing_warning} and stores the affected
raw names in its \code{instruments} field.
}

\examples{
\dontrun{
plan_instruments <- all_instruments(rcon)
extended_schedule <- build_extended_schedule(
  rcon,
  instruments = c("followup", "diary"),
  n_repeat_instances = 3L
)
plan <- plan_from_data(
  records,
  rcon,
  instruments = plan_instruments,
  extended_schedule = extended_schedule
)
}

}
\seealso{
\code{\link[=all_instruments]{all_instruments()}}, \code{\link[=plan_from_data]{plan_from_data()}}
}
