% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flex.R
\name{flex}
\alias{flex}
\alias{flex.redcapmissing}
\title{Format a REDCap missingness report as a flextable}
\usage{
flex(x, ...)

\method{flex}{redcapmissing}(x, events = NULL, forms = NULL, validation_check = NULL, ...)
}
\arguments{
\item{x}{A \code{redcapmissing} object created by \code{\link[=find_missing]{find_missing()}}.}

\item{...}{Unused.}

\item{events}{Optional character vector of raw REDCap \code{redcap_event_name}
values. When supplied, \code{flex()} returns only matching event rows.}

\item{forms}{Optional character vector of raw REDCap form/instrument names.
When supplied, \code{flex()} returns only matching form rows.}

\item{validation_check}{Optional character vector of raw validation-check
values from \code{\link[=tidy.redcapmissing]{tidy.redcapmissing()}}, such as \code{"field-complete"}. When
supplied, \code{flex()} returns only matching validation-check rows.}
}
\value{
A \code{flextable} object with labeled event/form context,
human-readable validation check, and pass/fail counts for display. Repeat
instrument and instance columns are included only when the displayed rows
contain repeat context. This function requires the optional \code{flextable}
and \code{glue} packages.
}
\description{
\code{flex()} formats the tidy validation summary from a REDCap missingness
report as a \code{flextable} for reporting workflows.
}
\details{
\code{flex()} is a display wrapper around \code{\link[=tidy.redcapmissing]{tidy.redcapmissing()}}. It preserves
one row per validation summary context, applies REDCap event and form labels
for display, and formats passed/failed counts with percentages. The optional
\code{events}, \code{forms}, and \code{validation_check} arguments filter by raw values
present in \code{tidy(x)} before labels are applied.

Use \code{flex_event_forms()} when you want the reduced event/form report with
form rows nested under event header rows instead of the full validation-check
summary.
}
\examples{
\dontshow{if (requireNamespace("flextable", quietly = TRUE) && requireNamespace("glue", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# After building a report with find_missing():
# flex(report)
# flex(report, validation_check = "field-complete")
# flex(report, events = "baseline_event", forms = "baseline_form")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=find_missing]{find_missing()}}, \code{\link[=tidy.redcapmissing]{tidy.redcapmissing()}}, \code{\link[=flex_event_forms]{flex_event_forms()}},
\code{\link[=flex_html]{flex_html()}}
}
