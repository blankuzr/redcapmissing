% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare-reports.R
\name{compare_reports}
\alias{compare_reports}
\alias{print.redcapmissing_comparison}
\title{Compare REDCap assessments within their validation contexts}
\usage{
compare_reports(previous, current)

\method{print}{redcapmissing_comparison}(x, ...)
}
\arguments{
\item{previous, current}{Reports created with \code{details = TRUE} and versioned
field-selection settings. Project identity, structure fingerprints and
normalized field-selection settings must match. Targets and verification
setups may differ. Older reports must be regenerated before comparison.}

\item{x}{A \code{redcapmissing_comparison} from \code{\link[=compare_reports]{compare_reports()}}.}

\item{...}{Passed to the tibble print method.}
}
\value{
A \code{redcapmissing_comparison} list with these components:
\itemize{
\item \code{plans}: the previous and current assessment plans.
\item \code{settings}: common \code{schema_version}, \code{required_fields}, \code{ignore_fields},
and \code{exclude_types}. Selection vectors are sorted sets; empty selections
use \code{character()}.
\item \code{target_results}: the union of target keys (\code{record_id}, \code{instrument},
\code{redcap_event_name}, \code{repeat_instrument}, \code{repeat_instance}), \code{target_scope}
(\code{"shared"}, \code{"added"}, \code{"removed"}), and every remaining target-result
column prefixed with \code{previous_} or \code{current_}. Absent-side values are
typed missing. Provenance does not determine target identity.
\item \code{summary}: see \strong{Validation summary} below.
\item \code{changes}: see \code{\link[=get_changes]{get_changes()}}.
\item \code{scope_changes}: added and removed rows of \code{target_results}, including
targets with no failures.
\item \code{verification}: \code{previous} and \code{current} lists with \code{enabled} and
\code{verified_user}, and a logical \code{setup_changed} flag.
}

\code{x}, invisibly.
}
\description{
Compare stored outcomes from two \code{\link[=run_plan]{run_plan()}} reports. Full-scope results
describe each assessment as run; shared-target results restrict both sides
to the exact targets present in both plans, without rerunning branching logic.
}
\section{Validation summary}{

Each row is keyed by \code{population} (\code{"full"} or \code{"shared"}),
\code{redcap_event_name}, \code{instrument}, \code{repeat_instrument}, \code{repeat_instance},
\code{validation_level}, and \code{validation_check}. Counts for the first three checks
count target outcomes; \code{field-complete} counts assessed field occurrences,
with each checkbox root counted once. Checks retain \code{\link[=registry]{registry()}} order.

\code{previous_in_scope} and \code{current_in_scope} are logical flags. Each side has
prefixed \code{status}, \code{reason}, \code{assessed}, \code{passed}, \code{failed}, \code{pass_rate}, and
\code{fail_rate}, using \code{\link[=get_summary]{get_summary()}} types and definitions. An absent context
has zero counts and missing status, reason and rates. Zero assessed counts
retain missing rates. \code{delta_assessed} and \code{delta_failed} are integer
current-minus-previous counts; \code{delta_fail_rate} is a double difference in
proportions, displayed as percentage points by \code{\link[=flexify]{flexify()}}.

Seven integer columns count \code{newly_detected}, \code{still_missing}, \code{completed},
\code{verified}, \code{no_longer_assessed}, \code{added_to_scope}, and \code{removed_from_scope}
transitions in that same stratum and unit. Shared-target rows have zero
scope-transition counts. Previously failed checks reconcile to still
missing, completed, verified, no longer assessed, and removed from scope;
current failures reconcile to still missing, newly detected, and added to
scope. Different checks must not be summed into one issue count.

Both populations retain the original assessment rules. Even shared targets
can have different field denominators when instrument start or branching
changes. Verification setup changes are recorded without assigning causation.
}

\section{Ordering and storage}{

Contexts follow first appearance in the previous plan, then current-only
contexts; checks follow registry order. Within each context/check, existing
issue order is retained and current-only issues follow. Record identifiers
remain character, including leading zeros, and nullable keys match by their
typed missing values. Tables carry union-scope \code{redcapmissing_labels}.
Response values, source reports, connections, and raw verification history
are not copied into the comparison. Record identifiers and available REDCap
links remain sensitive in the same way as the source reports.
}

\section{Conditions}{

Invalid, inconsistent, incompatible, legacy, or nondetailed inputs raise
\code{redcapmissing_error_comparison} with a recovery message.
}

\examples{
\dontrun{
comparison <- compare_reports(previous, current)
get_summary(comparison, population = "shared")
get_changes(comparison, validation_check = "field-complete")
flex_event_instruments(comparison)
}
}
\seealso{
\code{\link[=get_summary]{get_summary()}}, \code{\link[=get_changes]{get_changes()}}, \code{\link[=flex_event_instruments]{flex_event_instruments()}}
}
