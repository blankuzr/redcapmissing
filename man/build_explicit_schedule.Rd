% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-schedules.R
\name{build_explicit_schedule}
\alias{build_explicit_schedule}
\title{Build a project-aware explicit assessment schedule}
\usage{
build_explicit_schedule(data, rcon, explicit_spec)
}
\arguments{
\item{data}{A data frame containing the exact project record-ID field
discovered from \code{rcon}. Other columns are ignored. Record IDs are
normalized to character and treated as a set, retaining first-appearance
order.}

\item{rcon}{A \code{redcapAPI} connection inheriting from
\code{redcapApiConnection}, as created by \code{\link[redcapAPI:redcapConnection]{redcapAPI::redcapConnection()}}, or
\code{redcapOfflineConnection}, as created by \code{\link[redcapAPI:offlineConnection]{redcapAPI::offlineConnection()}}
or \code{\link[redcapAPI:readPreservedProject]{redcapAPI::readPreservedProject()}}. It must expose project
information, metadata, instruments, and applicable arms, events,
mappings, and repeat configuration.}

\item{explicit_spec}{A data frame containing exactly one event alias,
either \code{unique_event_name} or \code{redcap_event_name}, and exactly one
instrument alias, either \code{form} or \code{instrument}. An optional
\code{repeat_instance} column supplies positive repeat instances. When it is
absent, missing instances are used. Other columns are ignored, which
permits filtered rows from \code{rcon$mapping()} to be supplied directly.}
}
\value{
An ordinary tibble with exactly four columns: character
\code{record_id}, character \code{instrument}, character \code{redcap_event_name}, and
integer \code{repeat_instance}, in that order. If \code{data} has no records or
\code{explicit_spec} has no rows, a correctly typed zero-row tibble is
returned.
}
\description{
\code{build_explicit_schedule()} crosses a set of project record IDs with an
explicit specification of allowable REDCap instrument-event targets. It
discovers the project's record-ID field from \code{rcon}, so callers can pipe
project-shaped cohort data directly into the builder without renaming that
field to \code{record_id} first.
}
\details{
Each normalized record ID is crossed with every row of \code{explicit_spec}.
Output uses record-first order and preserves specification row order within
each record. Duplicate normalized specification rows are rejected.

The specification is checked against the current project structure. Raw
instruments and events must exist and form an allowable crossing. Classic
projects require missing events. Longitudinal projects require raw event
names. Repeating crossings require a supplied positive instance, whereas
nonrepeating crossings require a missing instance.

The function reads structural surfaces from \code{rcon} but never exports
records. \code{\link[=plan_explicit]{plan_explicit()}} revalidates the complete schedule and remains
responsible for checking consistency between scheduled record arms and the
physical \code{data} passed to that planner.
}
\section{Conditions}{

Argument, schema, project, and schedule failures inherit from
\code{redcapmissing_error}. Expansion is rejected before allocation if its
Cartesian row count exceeds R's integer row limit.
}

\examples{
\dontrun{
explicit_spec <- rcon$mapping() |>
  dplyr::filter(unique_event_name \%in\% desired_events)

explicit_schedule <- data.frame(
  participant_id = c("001", "002", "003")
) |>
  build_explicit_schedule(rcon, explicit_spec)

explicit_plan <- plan_explicit(
  records,
  rcon,
  explicit_schedule = explicit_schedule
)
}

}
\seealso{
\code{\link[=all_instruments]{all_instruments()}}, \code{\link[=build_extended_schedule]{build_extended_schedule()}},
\code{\link[=plan_explicit]{plan_explicit()}}
}
