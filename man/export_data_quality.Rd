% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-data-quality.R
\name{export_data_quality}
\alias{export_data_quality}
\title{Export Data Resolution Workflow history}
\usage{
export_data_quality(rcon, records = NULL, prefix = "data_quality_api")
}
\arguments{
\item{rcon}{A live \code{redcapApiConnection}, as created by
\code{\link[redcapAPI:redcapConnection]{redcapAPI::redcapConnection()}}. Its project information supplies the
project ID. Offline connections cannot retrieve history.}

\item{records}{\code{NULL} to retrieve all records, or a character vector of
nonmissing, nonblank, unpadded record IDs. Character leading zeros are
preserved. \code{character(0)} returns an empty table without requesting history.}

\item{prefix}{One nonmissing, nonblank, unpadded module prefix. The default
is \code{"data_quality_api"}.}
}
\value{
A plain \code{data.frame} with one row per resolution. Issue fields are
repeated on each row. An issue without resolutions contributes one row
with missing resolution values. All columns are character; JSON nulls and
absent optional fields become \code{NA_character_}. Timestamps retain their
source text. No reviewer, status, or assessment-context filtering occurs.

The following columns are always present, including in empty results:
\itemize{
\item Issue fields: \code{status_id}, \code{rule_id}, \code{pd_rule_id}, \code{non_rule},
\code{project_id}, \code{record}, \code{event_id}, \code{field_name}, \code{repeat_instrument},
\code{instance}, \code{status}, \code{exclude}, \code{query_status}, \code{assigned_username},
\code{group_id}.
\item Resolution fields: \code{res_id}, \code{ts}, \code{response_requested}, \code{response},
\code{comment}, \code{current_query_status}, \code{upload_doc_id},
\code{field_comment_edited}, \code{username}.
}

Additional scalar module fields are retained. No deployment-specific
additions, such as \code{event_name}, are required. Row order follows the
response and does not imply chronological order. The export can contain
comments and responses; store it according to project data-handling rules.
}
\description{
Retrieve and flatten the history provided by Vanderbilt's Data Quality API
external module. The result can be passed directly to \code{\link[=run_plan]{run_plan()}} as
\code{verified}; reviewer and latest-status selection happen during assessment.
}
\section{Prerequisites and retrieval}{

The project must use REDCap's Data Resolution Workflow. The Data Quality API
external module must be enabled in both the REDCap Control Center and the
project. Find its prefix in the module's configuration/link, or obtain it
from the REDCap administrator. Use a connection authorized to export the
project's workflow history.

\code{records = NULL} can be expensive for large projects. Restrict records when
useful, export once, and reuse the table across assessments. \code{\link[=run_plan]{run_plan()}}
never retrieves history itself. The connection's existing retry settings
apply. No status or user filter is sent: every resolution for the selected
issues is retained, including later entries that revoke verification.
}

\section{Conditions}{

Invalid arguments, failed requests, malformed responses, inconsistent
issue/resolution identities, and responses outside the requested project or
record scope raise \code{redcapmissing_error_export}, inheriting from
\code{redcapmissing_error}. Error messages exclude response contents. For request
or JSON failures, check module enablement, prefix, and connection permissions.
}

\examples{
\dontrun{
history <- export_data_quality(rcon, records = c("1", "2"))
report <- run_plan(plan, data, rcon,
  verified = history, verified_user = "reviewer"
)
}
}
\seealso{
\code{\link[=run_plan]{run_plan()}},
\href{https://github.com/vanderbilt-redcap/data_quality_api/blob/master/README.md}{Vanderbilt module documentation}
}
