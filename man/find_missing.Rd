% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find-missing.R
\name{find_missing}
\alias{find_missing}
\title{Build a branching-aware REDCap missingness report}
\usage{
find_missing(
  data,
  rcon,
  forms,
  events = NULL,
  records = NULL,
  required_fields = TRUE,
  ignore_fields = NULL,
  ignore_ids = NULL,
  exclude_types = "descriptive",
  instances = NULL,
  details = FALSE,
  progress = interactive()
)
}
\arguments{
\item{data}{A data frame or tibble of REDCap records, usually produced by
\code{redcapAPI::exportRecordsTyped()}. For longitudinal projects, this should
include the REDCap system column \code{redcap_event_name}. For repeating
instruments/events, it should include \code{redcap_repeat_instrument} and
\code{redcap_repeat_instance} when those columns are present in the export.}

\item{rcon}{A \code{redcapAPI::redcapConnection()} object, or an offline or
preserved connection-like object with the same methods. It must provide
project metadata through \code{rcon$metadata()} and instrument labels through
\code{rcon$instruments()}; form-event mapping, repeat structure, and project
information are used when available.}

\item{forms}{Required character vector of REDCap form/instrument names to
assess. No default is supplied so callers must choose the form or forms
deliberately. Duplicate form names are not allowed.}

\item{events}{A character vector of raw REDCap event names applied to all
requested forms, a named list of event vectors by form, or \code{NULL}. \code{NULL}
and omitted or \code{NULL} list entries select every event where the form is
offered. List names must be unique requested forms, and values cannot be
empty vectors. Values for a form offered on only one event are ignored. For
forms that are regular on some events and repeating on others, the selected
events determine whether repeat-instance checks apply. Defaults to \code{NULL}.}

\item{records}{A fully named list of record eligibility by raw
\code{redcap_event_name}, or \code{NULL}. An event-level vector applies to every
requested form and selected instance; an event/form vector applies only to
that form; and an event/form/instance list applies only to the named
positive whole-number instance. Names must be unique; event names must be
known project events, and nested names must identify requested forms and
valid contexts. Omitted contexts use eligibility derived from \code{data} and
the other selection arguments. Each supplied value must contain only
non-blank record IDs; \code{NULL}, empty, \code{NA}, and blank values error. IDs are
normalized to character and may be absent from \code{data}, in which case they
can produce row-started failures. Defaults to \code{NULL}, which applies no
record restriction.}

\item{required_fields}{Logical scalar. When \code{TRUE}, only fields marked as
required in the REDCap metadata \code{required_field} column are assessed. When
\code{FALSE}, all fields on the form are assessed after \code{exclude_types} and
\code{ignore_fields} are applied. Defaults to \code{TRUE}.}

\item{ignore_fields}{Character vector of REDCap field names to skip. Values
may be root field names such as \code{"checkbox_field"} or exported checkbox
child names such as \code{"checkbox_field___1"}; checkbox child names are mapped
back to their root field and the whole checkbox question is skipped.
Defaults to \code{NULL}, which means no fields are ignored.}

\item{ignore_ids}{Character vector of REDCap record IDs to skip. Rows whose
record identifier is in this vector are excluded before the missingness
report is built. Defaults to \code{NULL}, which means no records are ignored.}

\item{exclude_types}{Character vector of REDCap field types to exclude from
assessment. Defaults to \code{"descriptive"} because descriptive fields do not
capture values. These types are excluded regardless of \code{required_fields}.}

\item{instances}{A positive whole-number scalar count, vector of positive
whole-number instance IDs, named list by form, or \code{NULL}. A scalar such as
\code{2L} requests instances \code{1} and \code{2}; a longer vector such as \code{c(2L, 3L)}
requests exactly those IDs. List names must be unique requested forms;
omitted or \code{NULL} entries use the default. A repeating form without an
explicit setting warns once and uses instance \code{1}. Global values affect
only requested repeating contexts; a named non-\code{NULL} setting for a form
with no requested repeating context errors. Defaults to \code{NULL}.}

\item{details}{Logical scalar. When \code{FALSE}, the report omits full
validation-row tables and check-specific row tables. When \code{TRUE}, these
row-level tables are stored under \code{report$details}. Defaults to \code{FALSE}.}

\item{progress}{Logical scalar. When \code{TRUE}, displays processing progress by
form; percentages do not represent data completeness. Defaults to
\code{interactive()}.}
}
\value{
A list with class \code{"redcapmissing"} containing:
\describe{
\item{\code{summary}}{Compact validation summaries used by \code{\link[=get_summary]{get_summary()}}.}
\item{\code{missing}}{Compact failed-validation rows used by \code{\link[=get_missing]{get_missing()}}.}
\item{\code{spec}}{Normalized report configuration and REDCap context used by
report methods. \code{spec$record_eligibility} contains every final assessed
record/event/form/repeat-instance context, including when \code{records} is
omitted.}
\item{\code{diagnostics}}{Timing and row-count metadata, preserving the report
start/finish, elapsed time, form count, validation-row count, summary-row
count, and missing-row count. \code{diagnostics$stage_timings} is a tibble
with \code{scope}, \code{form}, \code{stage}, and \code{elapsed_seconds}; report stages
separate connection metadata/project-context access, plan compilation,
URL construction, and report assembly, while form stages separate
context, eligibility, row checks, blankness, form startedness, the four
field partitions, and form assembly. \code{diagnostics$form_workload} is a
per-form tibble with record/context/started-row counts, total and
assessable field counts, the four field-partition counts, and validation
rows.}
\item{\code{details}}{\code{NULL} by default. When \code{details = TRUE}, a list with
\code{validation_rows}, \code{checks}, and \code{failures} row tables.}
}
}
\description{
\code{find_missing()} checks expected REDCap event, repeat, form, and field
contexts for missingness. Expectations come from project metadata supplied
through \code{rcon} and the validation taxonomy returned by \code{\link[=registry]{registry()}}.
}
\details{
A field is expected on rows where its requested form is offered and its
REDCap branching logic, if any, evaluates to \code{TRUE}. Blank and \code{NA} values
are missing.

Checkbox fields are treated as one REDCap root field. A checkbox root is
answered when at least one exported child column (\code{field___choice}) is
selected; unselected siblings are not separately flagged.

A valid requested form remains in the report when \code{required_fields},
\code{exclude_types}, and \code{ignore_fields} leave no fields to assess.
\code{form-started} is still evaluated from the form's data-capturing metadata,
while \code{field-complete} contributes no assessments for that form.

The checks are assessed in registry order. Failed checks remove the
record/event/repeat/form context from downstream assessment. The function
stops if no record IDs remain after filtering, except that explicit \code{records}
entries can declare expected contexts for IDs absent from \code{data}; those
contexts can fail a row-started check.

Set \code{details = TRUE} to retain row-level check tables for troubleshooting.
See \code{vignette("redcapmissing", package = "redcapmissing")} for synthetic
event, record-eligibility, and repeat-instance workflows.
}
\examples{
\dontrun{
rcon <- redcapAPI::redcapConnection(
  url = Sys.getenv("REDCAP_API_URL"),
  token = Sys.getenv("REDCAP_API_TOKEN")
)

records <- redcapAPI::exportRecordsTyped(
  rcon,
  cast = list(
    radio = redcapAPI::castCode,
    dropdown = redcapAPI::castCode,
    yesno = redcapAPI::castCode,
    checkbox = redcapAPI::castRaw,
    system = redcapAPI::castRaw
  )
)

report <- find_missing(
  data = records,
  rcon = rcon,
  forms = "baseline_form",
  events = c("baseline_event", "followup_event"),
  ignore_fields = c("status_flag", "screening_code")
)

get_summary(report)
get_missing(report)

repeat_missing <- find_missing(
  data = records,
  rcon = rcon,
  forms = "repeat_form",
  instances = 2L
)
}

}
\seealso{
\code{\link[=get_summary]{get_summary()}}, \code{\link[=get_missing]{get_missing()}}, \code{\link[=flexify]{flexify()}}, \code{\link[=registry]{registry()}},
\code{\link[redcapAPI:redcapConnection]{redcapAPI::redcapConnection()}}, \code{\link[redcapAPI:exportRecordsTyped]{redcapAPI::exportRecordsTyped()}}
}
