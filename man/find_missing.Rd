% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find-missing.R
\name{find_missing}
\alias{find_missing}
\title{Build a branching-aware REDCap missing-field report}
\usage{
find_missing(
  data,
  rcon,
  form,
  desired_events = NULL,
  required_fields = TRUE,
  ignore_fields = NULL,
  ignore_ids = NULL,
  exclude_types = "descriptive",
  expected_repeats = NULL
)
}
\arguments{
\item{data}{A data frame or tibble of REDCap records, usually produced by
\code{redcapAPI::exportRecordsTyped()}. For longitudinal projects, this should
include the REDCap system column \code{redcap_event_name}. For repeating
instruments/events, it should include \code{redcap_repeat_instrument} and
\code{redcap_repeat_instance} when those columns are present in the export.}

\item{rcon}{A \code{redcapAPI::redcapConnection()} object, or an offline or
preserved connection-like object that mirrors the same methods, and that
provides project metadata through \code{rcon$metadata()}. When available,
form-event mapping, repeating instrument/event metadata, and project
information are also read from the connection object.}

\item{form}{Required. A single REDCap form/instrument name to assess. No
default is supplied so callers must choose the form deliberately.}

\item{desired_events}{Character vector of REDCap event names to assess for
the requested form, or \code{NULL}. When a form is offered on multiple REDCap
events, this argument can restrict the report to a selected subset of those
events. If \code{NULL}, all offered events are assessed. If the form is offered
on only one event, this argument is ignored. If the form is regular on
some events and repeating on others, \code{desired_events} also determines
whether repeat-instance logic is activated. Defaults to \code{NULL}.}

\item{required_fields}{Logical scalar. When \code{TRUE}, only fields marked as
required in the REDCap metadata \code{required_field} column are assessed. When
\code{FALSE}, all fields on the form are assessed after \code{exclude_types} and
\code{ignore_fields} are applied. Defaults to \code{TRUE}.}

\item{ignore_fields}{Character vector of REDCap field names to skip. Values
may be root field names such as \code{"checkbox_field"} or exported checkbox
child names such as \code{"checkbox_field___1"}; checkbox child names are mapped
back to their root field and the whole checkbox question is skipped.
Defaults to \code{NULL}, which means no fields are ignored.}

\item{ignore_ids}{Character vector of REDCap record IDs to skip. Rows whose
record identifier is in this vector are excluded before the missingness
report is built. Defaults to \code{NULL}, which means no records are ignored.}

\item{exclude_types}{Character vector of REDCap field types to exclude from
assessment. Defaults to \code{"descriptive"} because descriptive fields do not
capture values. These types are excluded regardless of \code{required_fields}.}

\item{expected_repeats}{Positive whole-number scalar, or \code{NULL}. When \code{form}
is assessed in a REDCap repeating event or as a repeating instrument, this
is the minimum number of repeat instances expected for every assessed
record/event context. For example, \code{expected_repeats = 2L} checks that
repeat instances \code{1} and \code{2} exist. If the form is repeating and this
argument is \code{NULL}, the function warns and assumes \code{1L} for the requested
repeating contexts. If the requested events do not include any repeating
contexts for the form, this argument is ignored.}
}
\value{
A list with class \code{"redcapmissing"} containing:
\describe{
\item{\code{agent}}{An interrogated \code{pointblank} agent.}
\item{\code{missing}}{A tibble of failed rows from the \code{pointblank} extract,
keyed by record, event, repeat context, form, and field.}
\item{\code{expected}}{The expected-field table checked by \code{pointblank}.}
\item{\code{validation_rows}}{The full table supplied to \code{pointblank}, including
field-level checks, any-field roll-up checks, blank-form checks,
missing-event checks, and missing-repeat checks.}
\item{\code{form_missing}}{One-row-per-context checks for exported form rows
where every form field is blank or unchecked.}
\item{\code{form_checks}}{All whole-form startedness checks, including passed
and failed form contexts.}
\item{\code{event_missing}}{One-row-per-context checks for longitudinal events
where the form is offered but no event row exists in the export.}
\item{\code{event_checks}}{All event-row presence checks, including passed and
failed record/event contexts.}
\item{\code{repeat_missing}}{One-row-per-context checks for expected repeat
instances that are absent from the export.}
\item{\code{repeat_checks}}{All expected repeat-instance checks, including
passed and failed repeat contexts.}
\item{\code{any_field_missing}}{One-row-per-context checks where at least one
expected field is blank.}
\item{\code{any_field_checks}}{All any-field roll-up checks, including passed
and failed evaluable contexts.}
\item{\code{field_plan}}{The metadata-derived field plan for the requested
form, including checkbox child export columns.}
\item{\code{required_fields}}{Whether the report was limited to REDCap-required
fields.}
\item{\code{desired_events}}{The REDCap events actually used for assessment, or
\code{character(0)} when no event restriction applied.}
\item{\code{expected_repeats}}{The repeat-instance count used for repeating
forms, or \code{NULL} for non-repeating forms.}
\item{\code{ignored_fields}}{Root field names skipped because of
\code{ignore_fields}.}
\item{\code{ignored_ids}}{Record IDs skipped because of \code{ignore_ids}.}
\item{\code{project}}{A compact list of REDCap project structure inferred from
\code{rcon}.}
\item{\code{form}}{The assessed form name.}
\item{\code{id_col}}{The REDCap record identifier field.}
\item{\code{system_fields}}{The REDCap system field names used internally.}
}
}
\description{
\code{find_missing()} checks whether expected REDCap fields are present
for a single instrument/form. Expected fields are determined from REDCap
project metadata and project structure supplied through a
\code{redcapAPI::redcapConnection()} workflow.
}
\details{
A field is expected when it is on the requested form and, if it has REDCap
branching logic, that branching logic evaluates to \code{TRUE} for the record row.
Fields without branching logic are always expected on rows where the form is
offered. Blank values and \code{NA} values are considered missing.

The function relies on \code{redcapAPI} project structure exposed through \code{rcon}
to avoid checking a form on events or repeating rows where that form is not
offered.
It inspects available connection methods such as \code{rcon$metadata()},
\code{rcon$mapping()} / \code{rcon$mappings()}, \code{rcon$repeatInstrumentEvent()}, and
\code{rcon$projectInformation()} when present.

Checkbox fields are treated as one REDCap root field. A checkbox root is
considered answered when at least one exported child column
(\code{field___choice}) is selected. Unselected sibling checkboxes are not flagged
as missing once the root question has at least one selected child.

If an offered form has no data entered at all, the report emits one form-level
missing row instead of one missing row per field. Longitudinal event rows that
are absent from the export are also emitted as one form-level missing row for
that record and event.

Validation is intentionally split between REDCap-specific R preprocessing and
\code{pointblank} reporting. The R code determines each row's \code{missing_scope}
before \code{pointblank} runs:
\describe{
\item{\code{"form_blank"}}{An exported row exists for the form context, but every
exported data-capturing field on that form is blank or unchecked. The row
is emitted with \code{form_started = FALSE}, \code{event_row_present = TRUE}, and
\code{value_present = TRUE}, so it fails only the whole-form pointblank check.}
\item{\code{"event_absent"}}{The form is mapped to a longitudinal event for a
record, but no exported row exists for that record/event. The row is
emitted with \code{event_row_present = FALSE}, \code{form_started = TRUE}, and
\code{value_present = TRUE}, so it fails only the missing-event pointblank
check.}
\item{\code{"repeat_absent"}}{The form is assessed in a REDCap repeating event
or as a repeating instrument, and an expected repeat instance is absent
for the record/event context. The row is emitted with
\code{repeat_instance_present = FALSE},
\code{form_started = TRUE}, \code{event_row_present = TRUE}, and
\code{value_present = TRUE}, so it fails only the missing-repeat pointblank
check.}
\item{\code{"any_field_missing"}}{The form context passed event, repeat, and
whole-form checks, but at least one expected field in that context is
blank. The row fails when \code{value_present} is \code{FALSE}.}
\item{\code{"field"}}{The form context exists and is started. A field is expected
after branching logic, required-field filtering, type exclusions, and
ignored fields are applied. The row fails only when \code{value_present} is
\code{FALSE}.}
}

The whole-form checks are built before field-level checks. Contexts already
classified as \code{form_blank} are removed from field-level assessment so the
report returns one form-level row instead of one row per missing field.
\code{pointblank} does not rediscover REDCap branching, blank forms, or absent
events; it provides the auditable validation steps and failed-row extraction
after those REDCap-specific rules have been encoded.

The pointblank checks are scoped with preconditions and stratified by a
\code{validation_context} so the agent summary denominator (\code{n}) is clinically
meaningful for each event or repeat instance. Context-level checks are
counted over expected record contexts. Granular field checks are counted over
expected field rows within each context. The \code{any_field_missing} check rolls
those field rows up to one row per evaluable record context.

The returned \code{pointblank} agent is interrogated with failed-row extraction
enabled. The row-level extract is also returned as \code{missing} for easier
review and downstream correction workflows.
}
\examples{
\dontrun{
rcon <- redcapAPI::redcapConnection(
  url = Sys.getenv("REDCAP_API_URL"),
  token = Sys.getenv("REDCAP_API_TOKEN")
)

records <- redcapAPI::exportRecordsTyped(
  rcon,
  cast = list(
    radio = redcapAPI::castCode,
    dropdown = redcapAPI::castCode,
    yesno = redcapAPI::castCode,
    checkbox = redcapAPI::castRaw,
    system = redcapAPI::castRaw
  )
)

baseline_missing <- find_missing(
  data = records,
  rcon = rcon,
  form = "baseline_form",
  desired_events = c("baseline_event", "followup_event"),
  ignore_fields = c("status_flag", "screening_code")
)

baseline_missing$agent
baseline_missing$missing

repeat_missing <- find_missing(
  data = records,
  rcon = rcon,
  form = "repeat_form",
  expected_repeats = 2L
)
}

}
\references{
Nutter B, Garbett S, Obregon S, Obadia T, Lehr M, High B, Lane S,
Beasley W, Gray W, Kennedy N, Hsi-Nien T, Horner J, Stephens J, Beck C,
Johnson B, Chase P, Tobias P (2026). \emph{redcapAPI: Accessing data from REDCap
projects using the API}. R package version 2.12.0.
\url{https://doi.org/10.5281/zenodo.10564837}.
}
\seealso{
\code{\link[redcapAPI:redcapConnection]{redcapAPI::redcapConnection()}}, \code{\link[redcapAPI:exportRecordsTyped]{redcapAPI::exportRecordsTyped()}}
}
