% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison-summary.R, R/report-accessors.R
\name{get_summary.redcapmissing_comparison}
\alias{get_summary.redcapmissing_comparison}
\alias{get_summary}
\alias{get_summary.redcapmissing}
\alias{get_summary.default}
\title{Get validation summaries from a REDCap missingness report}
\usage{
\method{get_summary}{redcapmissing_comparison}(
  report,
  validation_check = NULL,
  events = NULL,
  instruments = NULL,
  population = c("full", "shared"),
  ...
)

get_summary(
  report,
  validation_check = NULL,
  events = NULL,
  instruments = NULL,
  ...
)

\method{get_summary}{redcapmissing}(
  report,
  validation_check = NULL,
  events = NULL,
  instruments = NULL,
  ...
)

\method{get_summary}{default}(
  report,
  validation_check = NULL,
  events = NULL,
  instruments = NULL,
  ...
)
}
\arguments{
\item{report}{A \code{redcapmissing} report from \code{\link[=run_plan]{run_plan()}} or a
\code{redcapmissing_comparison} from \code{\link[=compare_reports]{compare_reports()}}.}

\item{validation_check}{\code{NULL}, or a nonempty character vector containing one
or more exact codes from \code{\link[=registry]{registry()}}: \code{"event-row-started"},
\code{"repeat-instance-row-started"}, \code{"instrument-started"}, or
\code{"field-complete"}.}

\item{events}{\code{NULL}, or a nonempty character vector of exact raw REDCap
unique event names represented by the plan. Classic project event context
is \code{NA_character_}; filters contain raw event names.}

\item{instruments}{\code{NULL}, or a nonempty character vector of exact raw REDCap
instrument names selected by the plan.}

\item{population}{For comparisons, \code{"full"}, \code{"shared"}, or both (the
default). Full scope uses each complete plan; shared targets use only
exact target keys present in both plans. See \code{\link[=compare_reports]{compare_reports()}} for the
comparison summary schema. Filters never recompute denominators.}

\item{...}{Arguments for methods. The comparison method accepts \code{population}.}
}
\value{
For a comparison, the stratified tibble documented in
\code{\link[=compare_reports]{compare_reports()}}. For a report, a tibble with exactly these columns
and storage types:\tabular{ll}{
   Column \tab Storage and values \cr
   \code{redcap_event_name} \tab Character raw event name; \code{NA_character_} in classic projects \cr
   \code{instrument} \tab Character raw instrument name \cr
   \code{repeat_instrument} \tab Character raw repeating instrument; otherwise \code{NA_character_} \cr
   \code{repeat_instance} \tab Integer exact instance; otherwise \code{NA_integer_} \cr
   \code{validation_level} \tab Character: \code{"event:instrument"} or \code{"event:instrument:instance"} \cr
   \code{validation_check} \tab Character validation check code \cr
   \code{status} \tab Character: \code{"assessed"} or \code{"not applicable"} \cr
   \code{reason} \tab Character; typed missing unless the check is not applicable \cr
   \code{assessed}, \code{passed}, \code{failed} \tab Integer counts \cr
   \code{pass_rate}, \code{fail_rate} \tab Double proportions; \code{NA_real_} when nothing was assessed \cr
}


The tibble has a \code{redcapmissing_labels} attribute containing named character
vectors \code{events} and \code{instruments} for presentation. Raw values remain in
the returned data and are used for filtering.
}
\description{
\code{get_summary()} returns the stored validation summary from a report created by
\code{\link[=run_plan]{run_plan()}}. Filters select rows from the stored summary. Assessment results,
applicability, and denominators remain those computed by \code{\link[=run_plan]{run_plan()}}.
}
\section{Filter semantics}{

Values within one non-\code{NULL} filter vector are alternatives: a stored row
matches that filter when its exact raw value equals any supplied value.
Multiple non-\code{NULL} filters are combined by intersection, so a row must
satisfy every supplied filter. Matching is case sensitive.

Filter vectors require present, nonblank, unpadded values. Unknown values
error. Duplicate values normalize to their first occurrence before
filtering. Filtering subsets the stored component without changing its row
order.

When no rows match, \code{get_summary()} retains the names, order, and storage
types documented for \code{report$summary}; \code{get_missing()} retains those
documented for \code{report$missing}. The \code{redcapmissing_labels} attribute
continues to contain labels for every event and instrument represented by
\code{report$plan}, including labels absent from the returned rows.
}

\examples{
\dontrun{
# report and instruments are caller supplied.
get_summary(report)
get_summary(
  report,
  validation_check = "field-complete",
  instruments = instruments
)
}

}
\seealso{
\code{\link[=get_missing]{get_missing()}}, \code{\link[=run_plan]{run_plan()}}, \code{\link[=registry]{registry()}}, \code{\link[=flexify]{flexify()}}
}
