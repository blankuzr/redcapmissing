% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flex-event-instruments.R
\name{flex_event_instruments}
\alias{flex_event_instruments}
\alias{flex_event_instruments.redcapmissing}
\title{Format a REDCap missingness report by event and instrument}
\usage{
flex_event_instruments(x, missing_threshold = 0.1, ...)

\method{flex_event_instruments}{redcapmissing}(x, missing_threshold = 0.1, ...)
}
\arguments{
\item{x}{A \code{redcapmissing} object created by \code{\link[=run_plan]{run_plan()}}.}

\item{missing_threshold}{A finite numeric scalar from zero through one.
Fractions must be strictly greater than thresholds below one. At one,
fractions equal to one are counted.}

\item{...}{Additional arguments passed to methods; currently unused.}
}
\value{
A \code{flextable} containing an \code{All} row, event headers, and instrument
rows. Repeat columns appear only when the plan contains repeat targets.
}
\description{
\code{flex_event_instruments()} formats a \code{\link[=run_plan]{run_plan()}} result as a reduced
event/instrument \code{flextable}. It reports started/due counts together with
instrument-level incomplete, not-started, and missing-threshold metrics.
}
\details{
Metrics are computed from \code{x$target_results}, which has one row per
Assessible target. Consequently, the table uses the frozen plan rather than
inferring opportunities from whichever rows happen to be present at run
time. Instrument metrics display \verb{N/D (\%)}; the \code{All} row sums instrument
opportunities without deduplicating records across instruments.

\verb{Instrument Incomplete} counts targets with a failed event-row,
repeat-instance-row, instrument-started, or field-complete check.
\verb{Instrument Not Started} counts targets that failed before field-complete.
For the threshold metric, an upstream or instrument-started failure has a
missing fraction of one. A started instrument uses \code{fields_failed / fields_assessed}; a not-applicable field-complete check has fraction zero.
}
\examples{
\dontrun{
plan <- plan_from_data(records, rcon, c("status", "survey"))
report <- run_plan(plan, records, rcon)
flex_event_instruments(report)
}

}
\seealso{
\code{\link[=run_plan]{run_plan()}}, \code{\link[=get_summary]{get_summary()}}, \code{\link[=flexify]{flexify()}}, \code{\link[=flex_html]{flex_html()}}
}
