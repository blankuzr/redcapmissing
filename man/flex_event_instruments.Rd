% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flex-event-instruments.R
\name{flex_event_instruments}
\alias{flex_event_instruments}
\alias{flex_event_instruments.redcapmissing}
\title{Format a REDCap missingness report by event and instrument}
\usage{
flex_event_instruments(x, missing_threshold = 0.1, ...)

\method{flex_event_instruments}{redcapmissing}(x, missing_threshold = 0.1, ...)
}
\arguments{
\item{x}{A \code{redcapmissing} object created by \code{\link[=run_plan]{run_plan()}}.}

\item{missing_threshold}{A finite numeric scalar from zero through one, with
comparison behavior described in \strong{Details}.}

\item{...}{Additional arguments passed to methods; currently unused.}
}
\value{
A \code{flextable} containing an \code{All} row, event headers, and instrument
rows. Repeat columns appear only when the plan contains repeat targets.
}
\description{
\code{flex_event_instruments()} formats a \code{\link[=run_plan]{run_plan()}} result as an event and
instrument \code{flextable}. It reports planned opportunities, started rows,
incomplete instruments, instruments that have not started, and missingness
above a selected threshold.
}
\details{
Metrics are computed from \code{x$target_results}, which has one row per
\code{assessible_targets} row. \code{x$plan$assessible_targets} defines every
denominator.
Instrument metrics display \verb{N/D (\%)}. Event header \code{N (started/due)} values
count unique record and event contexts: the numerator has a passed
\code{event-row-started} check and the denominator has an assessed event gate.
In a classic project the event gate is not applicable, so all planned record
contexts are shown as started and due.

On a repeating instrument or event row, \code{N (started/due)} is the number of
passed \code{repeat-instance-row-started} checks over the number assessed. It is
blank for instrument rows whose target context stores \code{NA_integer_} in
\code{repeat_instance}. \verb{Instrument Incomplete} counts targets with any failed
\code{event-row-started}, \code{repeat-instance-row-started}, \code{instrument-started}, or
\code{field-complete} check. \verb{Instrument Not Started} counts targets with a
failed event, repeat instance, or instrument start check.

For the threshold metric, an event, repeat instance, or instrument start
failure has a missing fraction of one. A started instrument uses
\code{fields_failed / fields_assessed}; a \code{field-complete} check with no
applicable fields has fraction zero. Fractions must exceed thresholds below
one; a threshold of one includes fractions equal to one. The \code{All} row sums
instrument opportunities and can count one record more than once when that
record has multiple planned instruments. Event header rows leave instrument
metrics blank.

The optional packages \code{flextable} and \code{glue} are required when this function
is called. The error lists each missing package.
}
\examples{
\dontrun{
# report is caller supplied.
flex_event_instruments(report)
}

}
\seealso{
\code{\link[=run_plan]{run_plan()}}, \code{\link[=get_summary]{get_summary()}}, \code{\link[=flexify]{flexify()}}, \code{\link[=flex_html]{flex_html()}}
}
