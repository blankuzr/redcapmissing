% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison-summary.R
\name{get_changes}
\alias{get_changes}
\title{Get record and field changes from an assessment comparison}
\usage{
get_changes(
  comparison,
  validation_check = NULL,
  events = NULL,
  instruments = NULL,
  change = NULL
)
}
\arguments{
\item{comparison}{A \code{redcapmissing_comparison} from \code{\link[=compare_reports]{compare_reports()}}.}

\item{validation_check}{\code{NULL}, or a nonempty character vector containing one
or more exact codes from \code{\link[=registry]{registry()}}: \code{"event-row-started"},
\code{"repeat-instance-row-started"}, \code{"instrument-started"}, or
\code{"field-complete"}.}

\item{events}{\code{NULL}, or a nonempty character vector of exact raw REDCap
unique event names represented by the plan. Classic project event context
is \code{NA_character_}; filters contain raw event names.}

\item{instruments}{\code{NULL}, or a nonempty character vector of exact raw REDCap
instrument names selected by the plan.}

\item{change}{\code{NULL} or a nonempty vector of exact transition names from
\strong{Transitions} below. Values within a filter are alternatives; different
filters intersect. Check, event, and instrument filters follow
\code{\link[=get_summary]{get_summary()}} rules against the union of both plan scopes.}
}
\value{
A tibble retaining all \code{\link[=get_missing]{get_missing()}} columns and their types, plus
character \code{validation_level}, \code{target_scope}, \code{change}, and \code{reason}.
Each side adds character \code{previous_raw_disposition} /
\code{current_raw_disposition} and \code{previous_effective_disposition} /
\code{current_effective_disposition}, and logical
\code{previous_verification_applied} / \code{current_verification_applied}.
Absent-side outcomes are typed missing. Unassessed fields on present
targets use \code{"not reached"} for upstream gating and \code{"not applicable"}
for closed branching. The \code{redcapmissing_labels} attribute retains labels
for the union of both scopes, including after filtering to zero rows.
}
\description{
Return failures present in either report, organized by validation context
and registry check order. This is the drill-down for \code{\link[=get_summary]{get_summary()}}.
}
\section{Transitions}{

\itemize{
\item \code{newly_detected}: currently fails and previously did not fail. \code{reason}
distinguishes newly assessed checks and verification that no longer applies.
\item \code{still_missing}: fails in both reports.
\item \code{completed}: previously failed and now passes directly.
\item \code{verified}: previously failed and now passes through verification.
\item \code{no_longer_assessed}: previously failed and is now blocked by branching or
an upstream gate; this is not completed data entry.
\item \code{added_to_scope} / \code{removed_from_scope}: a failure whose target entered or
left the plan. Scope membership takes precedence over outcome transitions.
}
}

\examples{
\dontrun{
get_changes(comparison, validation_check = "field-complete")
get_changes(comparison, change = c("newly_detected", "added_to_scope"))
}
}
\seealso{
\code{\link[=compare_reports]{compare_reports()}}, \code{\link[=get_summary]{get_summary()}}, \code{\link[=flexify]{flexify()}}
}
